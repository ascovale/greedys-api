package com.application.controller.Converter;
/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.core.convert.converter.Converter;

import com.application.persistence.model.Menu;
import com.application.serviceImpl.MenuService;

public class IdToMenuConverter implements Converter<String, Menu> {
	@Autowired
	MenuService menuService;

	public Menu convert(String id) {
		return this.menuService.findById(Long.valueOf(id));
	}
}*/