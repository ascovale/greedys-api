# Pipeline fix: risolto problema database connectivity - cleaned volumes
variables:
  TAG_LATEST: ${CI_REGISTRY_IMAGE}:latest
  TAG_COMMIT: ${CI_REGISTRY_IMAGE}:${CI_COMMIT_SHORT_SHA}
  DART_REPO: https://gitlab.com/PsychoOrange/greedys_api_dart_lib.git
  # Base URL delle spec OpenAPI - HTTP diretto senza SSL
  API_DOCS_BASE: "http://api.greedys.it/v3/api-docs"
  # 🆕 Base URL per response wrappers catalog - HTTP diretto
  WRAPPERS_ENDPOINT: "http://api.greedys.it/api/internal/response-wrapper-catalog"
  # Elenco dei gruppi da processare
  API_GROUPS: "restaurant admin customer"
  # 🆕 OpenAPI Generator CLI version per download diretto se necessario
  OPENAPI_GENERATOR_VERSION: "7.8.0"

stages:
  - build
  - deploy
  - publish

cache:
  paths:
    - .m2/repository
    - node_modules/
    - tools/  # 🆕 Cache per JAR e tools scaricati

build:
  image: docker:24.0.5
  stage: build
  services:
    - docker:24.0.5-dind
  variables:
    DOCKER_BUILDKIT: "1"
  script:
    - echo "Logging into Docker registry..."
    - echo $CI_JOB_TOKEN | docker login -u gitlab-ci-token --password-stdin $CI_REGISTRY
    - echo "Building and tagging Docker image..."
    - |
      for i in 1 2 3; do \
        docker build --pull --progress=plain -t $TAG_LATEST --build-arg SPRING_PROFILES_ACTIVE="docker" . && break || { echo "docker build failed (attempt $i/3), retrying in 10s"; sleep 10; }; \
      done
    - echo "Tagging Docker image with commit tag..."
    - docker tag $TAG_LATEST $TAG_COMMIT
    - echo "Pushing Docker images..."
    - docker push $TAG_LATEST
    - docker push $TAG_COMMIT
  only:
    - main
  after_script:
    - echo "Cleaning up Docker resources..."
    - docker system prune -f

deploy:
  image: alpine:3.18
  stage: deploy
  before_script:
    - apk add --no-cache openssh-client openssh-keygen
    # Crea cartella deploy per organizzare i file temporanei
    - mkdir -p deploy
    # se $ID_RSA è una variabile **File**:
    - cp "$ID_RSA" deploy/id_key
    - tr -d '\r' < deploy/id_key > deploy/id_key.clean && mv deploy/id_key.clean deploy/id_key
    - chmod 600 deploy/id_key
    - mkdir -p ~/.ssh && ssh-keyscan -H "$SERVER_IP" >> ~/.ssh/known_hosts
    # sanity: tipo e fingerprint
    - echo "== Key sanity ==" && head -1 deploy/id_key && tail -1 deploy/id_key
    - ssh-keygen -lf deploy/id_key
    # confronta con la chiave pubblica che hai messo su deployer
    - ssh-keygen -y -f deploy/id_key > deploy/id_key.pub
    - |
      echo "== Match con authorized_keys su deployer =="
      ssh -o BatchMode=yes -o IdentitiesOnly=yes -i deploy/id_key deployer@"$SERVER_IP" \
        "grep -F -x -q \"$(cat deploy/id_key.pub)\" ~/.ssh/authorized_keys && echo MATCH || echo NO_MATCH"
  script:
    - ssh -o IdentitiesOnly=yes -i deploy/id_key deployer@"$SERVER_IP" 'mkdir -p ~/greedys_api/'
    - scp -o IdentitiesOnly=yes -i deploy/id_key docker-compose.yml deploy.sh deployer@"$SERVER_IP":~/greedys_api/
    - printf '%s' "$CI_JOB_TOKEN" | ssh -o IdentitiesOnly=yes -i deploy/id_key deployer@"$SERVER_IP" "docker login -u gitlab-ci-token --password-stdin $CI_REGISTRY"
    - ssh -o IdentitiesOnly=yes -i deploy/id_key deployer@"$SERVER_IP" 'cd ~/greedys_api/ && chmod +x ./deploy.sh && ./deploy.sh'
  only: [main]


publish:
  stage: publish
  before_script:
    - echo "Setup ambiente per generazione Dart..."
    # Installa solo le dipendenze essenziali
    - apt-get update && apt-get install -y curl git maven
    
    # Installa OpenAPI Generator CLI
    - curl -sL https://deb.nodesource.com/setup_18.x | bash -
    - apt-get install -y nodejs
    - npm install -g @openapitools/openapi-generator-cli

    # BUILD DEL CUSTOM GENERATOR (solo se necessario)
    - echo "Controllo se serve buildare il custom generator..."
    - |
      if [ ! -d "generator" ]; then
        echo "❌ ERRORE: Generator custom non trovato in generator/"
        exit 1
      fi
      
      echo "✅ Generator trovato in generator/"
      cd generator
      
      # Controlla se il JAR esiste ed è aggiornato
      CUSTOM_JAR_PATTERN="target/custom-dart-codegen-*.jar"
      if ls $CUSTOM_JAR_PATTERN 1> /dev/null 2>&1 && [ "$CUSTOM_JAR_PATTERN" -nt "pom.xml" ]; then
        echo "✅ JAR custom già esistente e aggiornato"
        export CUSTOM_JAR="$(ls $CUSTOM_JAR_PATTERN | head -1)"
      else
        echo "🔨 Building custom generator..."
        mvn clean package -DskipTests -q
        if ls $CUSTOM_JAR_PATTERN 1> /dev/null 2>&1; then
          export CUSTOM_JAR="$(ls $CUSTOM_JAR_PATTERN | head -1)"
          echo "✅ Custom generator buildato: $CUSTOM_JAR"
        else
          echo "❌ ERRORE: JAR custom non trovato dopo build"
          exit 1
        fi
      fi
      cd ..

    # TROVA IL JAR DI OPENAPI-GENERATOR (con fallback)
    - |
      # Prova prima npm
      NPM_JAR_PATH="$(npm root -g)/@openapitools/openapi-generator-cli/bin/openapi-generator-cli.jar"
      if [ -f "$NPM_JAR_PATH" ]; then
        export NPM_OPENAPI_JAR="$NPM_JAR_PATH"
        echo "✅ OpenAPI Generator JAR trovato via npm: $NPM_OPENAPI_JAR"
      else
        echo "❌ JAR npm non trovato in $NPM_JAR_PATH"
        # Fallback: scarica direttamente
        mkdir -p tools
        DIRECT_JAR="tools/openapi-generator-cli.jar"
        if [ ! -f "$DIRECT_JAR" ]; then
          echo "📥 Scarico OpenAPI Generator CLI direttamente..."
          curl -sL "https://repo1.maven.org/maven2/org/openapitools/openapi-generator-cli/7.8.0/openapi-generator-cli-7.8.0.jar" -o "$DIRECT_JAR"
        fi
        export NPM_OPENAPI_JAR="$DIRECT_JAR"
        echo "✅ OpenAPI Generator JAR scaricato: $NPM_OPENAPI_JAR"
      fi
    - ls -la "$NPM_OPENAPI_JAR"

    # CONFIGURA COMANDO CUSTOM GENERATOR
    - echo "🎯 Configurazione CUSTOM generator"
    - echo "Custom JAR:" "$CUSTOM_JAR"
    - ls -la "$CUSTOM_JAR"
    - export OPENAPI_CUSTOM_CMD="java -cp $NPM_OPENAPI_JAR:$CUSTOM_JAR org.openapitools.codegen.OpenAPIGenerator generate"
    - echo "Comando configurato:" "$OPENAPI_CUSTOM_CMD"

    # Setup GIT con PAT (metodo unificato)
    - echo "echo \$PAT" > /tmp/git-askpass.sh
    - chmod +x /tmp/git-askpass.sh
    - export GIT_ASKPASS=/tmp/git-askpass.sh

    - echo "Clono il repo delle librerie Dart (greedys_api_dart_lib)…"
    - git clone https://gitlab.com/PsychoOrange/greedys_api_dart_lib.git
    - cd greedys_api_dart_lib
    - git config user.email "ci-bot@greedys.com"
    - git config user.name  "Greedys CI Bot"

  script:
    - echo "⏳ Attendo che l'API sia pronta dopo il deploy..."
    - echo "Controllo endpoint:" "${API_DOCS_BASE}/restaurant-api"
    - |
      for i in {1..50}; do
        echo "Tentativo $i/50: Testing ${API_DOCS_BASE}/restaurant-api"
        
        # Test più dettagliato con output degli errori
        if curl -f --max-time 15 --connect-timeout 10 -v "${API_DOCS_BASE}/restaurant-api" >/dev/null 2>&1; then
          echo "✅ API pronta! Procedo con la generazione client..."
          break
        else
          # Proviamo anche l'endpoint base per vedere se il server risponde
          if curl -f --max-time 10 --connect-timeout 5 "https://api.greedys.it" >/dev/null 2>&1; then
            echo "⚠️ Server risponde ma API docs non disponibili"
          else
            echo "❌ Server non risponde completamente"
          fi
        fi
        
        sleep 10
        if [ $i -eq 50 ]; then
          echo "❌ TIMEOUT: API non risponde dopo 8+ minuti"
          echo "Provo ultimo test di connettività..."
          curl -I --max-time 5 "https://api.greedys.it" || echo "Server completamente irraggiungibile"
          exit 1
        fi
      done
    
    - echo "Pulisco cartelle client API…"
    - rm -rf restaurant admin customer

    # GENERA PER OGNI GRUPPO con CUSTOM GENERATOR
    - |
      for GROUP in $API_GROUPS; do
        SPEC_FILE="./openapi-${GROUP}.json"
        OUTPUT_DIR="./${GROUP}"

        echo
        echo ">>> Generazione client per $GROUP <<<"
        echo "- Scarico spec da $API_DOCS_BASE/${GROUP}-api"
        curl -sSf -o "$SPEC_FILE" "${API_DOCS_BASE}/${GROUP}-api"

        # SCARICA RESPONSE WRAPPERS SPECIFICO PER GRUPPO
        echo "- Scarico catalogo response wrappers per gruppo $GROUP..."
        curl -sSf -o "generator/response-wrappers-${GROUP}.json" "$WRAPPERS_ENDPOINT/${GROUP}" || echo "⚠️ Catalogo wrappers per $GROUP non disponibile"

        # GENERA CON CUSTOM GENERATOR
        echo "- 🎯 Genero con CUSTOM generator in $OUTPUT_DIR"
        $OPENAPI_CUSTOM_CMD \
          -g org.openapitools.custom.CustomDartClientCodegen \
          -i "$SPEC_FILE" \
          -o "$OUTPUT_DIR" \
          -t "generator/templates" \
          --ignore-file-override "generator/.openapi-generator-ignore" \
          --skip-validate-spec \
          -p pubName=greedys_${GROUP}_api \
          -p pubVersion=1.0.0 \
          -p pubDescription="Greedys ${GROUP^} API client" \
          -p pubHomepage=https://greedys.it

        # POST-PROCESSING
        echo "- Post-processing per $GROUP..."
        
        # Rimuovi doc ResponseWrapper indesiderati
        [ -d "$OUTPUT_DIR/doc" ] && find "$OUTPUT_DIR/doc" -name "ResponseWrapper*.md" ! -name "ResponseWrapperErrorDetails.md" -delete 2>/dev/null || true
        
        # Rimuovi test ResponseWrapper indesiderati  
        [ -d "$OUTPUT_DIR/test" ] && find "$OUTPUT_DIR/test" -name "response_wrapper*.dart" ! -name "response_wrapper_error_details_test.dart" -delete 2>/dev/null || true
        
        # Pulisci import ResponseWrapper
        [ -d "$OUTPUT_DIR/lib" ] && find "$OUTPUT_DIR/lib" -name "*.dart" -exec sed -i "/^import 'package:.*\/response_wrapper.*';$/d" {} \; 2>/dev/null || true
        
        # Aggiorna pubspec.yaml con common_lib
        PUBSPEC="$OUTPUT_DIR/pubspec.yaml"
        if [ -f "$PUBSPEC" ] && ! grep -q "common_lib:" "$PUBSPEC"; then
          echo "  common_lib:" >> "$PUBSPEC"
          echo "    path: ../common" >> "$PUBSPEC"
        fi
        
        # Pulisci export ResponseWrapper da openapi.dart
        OPENAPI_FILE="$OUTPUT_DIR/lib/openapi.dart"
        [ -f "$OPENAPI_FILE" ] && sed -i "/^export 'package:.*\/response_wrapper\([^_]\|_[^e]\)/d" "$OPENAPI_FILE" 2>/dev/null || true

        echo "- Rimuovo spec temporanea"
        rm "$SPEC_FILE"
        
        echo "✅ Client $GROUP generato (post-processing sarà fatto dalla pipeline dedicata)"
      done

    - echo
    - echo "Committiamo e pushiamo le modifiche…"
    - git add .
    - |
      if git diff-index --quiet HEAD; then
        echo " Nessuna modifica da pushare."
      else
        # COMMIT MESSAGE - client base senza post-processing
        COMMIT_MSG="🤖 Genera Dart clients base: $API_GROUPS (post-processing in pipeline dedicata)"
        
        git commit -m "$COMMIT_MSG"
        git push origin main
        echo "✅ Client pushati con successo!"
      fi

  only:
    - main