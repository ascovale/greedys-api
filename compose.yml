services:
  spring-app:
    image: registry.gitlab.com/greedys_api/spring-app:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5050:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://db:3306/greedys_v1
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: Minosse100%
    depends_on:
      - db
      - rabbitmq
    networks:
      - app-network

  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: greedys_v1
      MYSQL_ROOT_PASSWORD: Minosse100%
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - app-network

  rabbitmq:
    image: rabbitmq:latest
    environment:
      - RABBITMQ_DEFAULT_USER=ascovale
      - RABBITMQ_DEFAULT_PASS=Sirio56asc!
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - app-network

networks:
  app-network:

volumes:
  db_data: