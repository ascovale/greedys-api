# Dynamic Configuration
http:
  middlewares:
    # ⭐ WebSocket Middleware - Permetti upgrade HTTP→WebSocket
    websocket:
      headers:
        customRequestHeaders:
          Upgrade: "websocket"
          Connection: "Upgrade"
          Sec-WebSocket-Version: "13"
    
    # CORS Middleware
    cors:
      headers:
        accessControlAllowMethods:
          - GET
          - POST
          - PUT
          - DELETE
          - OPTIONS
          - PATCH
        accessControlAllowOriginList:
          - "https://greedys.it"  # Il tuo sito principale
          - "https://www.greedys.it"  # Versione www
          - "https://app.greedys.it"  # Eventuale app frontend
        accessControlAllowHeaders:
          - "*"
        accessControlExposeHeaders:
          - "Authorization"
          - "Content-Type"
        accessControlAllowCredentials: true
        accessControlMaxAge: 86400
    
    # Security Headers
    security:
      headers:
        frameDeny: true
        sslRedirect: true
        browserXssFilter: true
        contentTypeNosniff: true
        forceSTSHeader: true
        stsIncludeSubdomains: true
        stsPreload: true
        stsSeconds: 31536000
        customRequestHeaders:
          X-Forwarded-Proto: "https"
    
    # Rate Limiting
    rate-limit:
      rateLimit:
        burst: 100
        average: 50
        period: 1m
    
    # Basic Auth per admin/dashboard (disabilitato per ora)
    # admin-auth:
    #   basicAuth:
    #     users:
    #       - "admin:$2y$10$yourhashpassword"
