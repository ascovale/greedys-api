{
  "info": {
    "_postman_id": "60-days-bulk-corrected",
    "name": "60-Day Bulk Reservations - CORRECTED SLOTS",
    "description": "Create 60 reservations (tomorrow to 60 days out) cycling through correct services and slot IDs",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Step 1: Login",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "console.log('\\n🔐 LOGIN');",
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    pm.environment.set('restaurantToken', jsonData.jwt);",
              "    pm.environment.set('restaurantId', jsonData.user.restaurantId);",
              "    console.log('✅ Token saved, Restaurant ID:', jsonData.user.restaurantId);",
              "}"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {"key": "Content-Type", "value": "application/json"}
        ],
        "body": {
          "mode": "raw",
          "raw": "{\"username\": \"test@test.it\", \"password\": \"TestPass123!\", \"rememberMe\": true}"
        },
        "url": {
          "raw": "{{baseUrl}}/restaurant/user/auth/login",
          "host": ["{{baseUrl}}"],
          "path": ["restaurant", "user", "auth", "login"]
        }
      }
    }